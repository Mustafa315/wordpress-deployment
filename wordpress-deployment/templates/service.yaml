apiVersion: v1
kind: Service
metadata:
  name: wp-service1
spec:
  selector:
    app: wp-dep1
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
  #externalIPs:
   # - 192.168.58.2


